# Notes Management App (React + Tailwind + localStorage)

A feature-rich notes app with pinning, archiving, and trash management.

## 🚀 Features
- Create, edit, delete notes
- Tag and search
- Pin, archive, and trash
- Data persists in localStorage
- Built with React Router + TailwindCSS

## 🧠 Tech Stack
React, TailwindCSS, React Router DOM, localStorage

## 🏗️ Setup
```bash
npm create vite@latest notes-app --template react
cd notes-app
npm install react-router-dom uuid
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
